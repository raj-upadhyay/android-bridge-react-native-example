1. Create custom component using only native library(BridgeModule.java)
2. Import all the components to a single package file(BridgeModulePackage.java)
3. Add package in native files(MainApplication.java)
4. Use the custom package from any React-native page inside app by importing NativeModules component from "react-native"